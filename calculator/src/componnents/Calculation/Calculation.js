import React from 'react';
import 'tachyons';
import './Calculation.css';

const Calculation = () => {
      return(
      <div id="calculations"  style={{display: 'flex', justifyContent: 'flex-end'}}>
        <p  id="nav-item"  className='f4 link dim pa1 pointer'>Calculations will be here!</p>
      </div>
          );
}

export default Calculation;
